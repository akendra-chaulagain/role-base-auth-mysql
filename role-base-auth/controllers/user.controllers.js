const db = require("../connection/db");
const bcrypt = require("bcrypt");
const jwt = require("jsonwebtoken");
const { roles } = require("../utils/constant");
// changeslmlfml;g,mg;,g

const deleteUser = async (req,res)=>{




}
module.exports = { deleteUser };
